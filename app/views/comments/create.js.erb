<% if @error %>

<% else %>
  $('#comments').prepend("<%= escape_javascript(render 'blogs/comments/show', comment: @comment) %>");
  // $(".redactor-comment").redactor('code.set', '');
  var comment_count = $(".comments-count").html();
  $(".comments-count").html(parseInt(comment_count) + 1);
<% end %>